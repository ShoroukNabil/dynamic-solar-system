import { Routes } from '@angular/router';
import { SolarSystemComponent } from './features/solar-system/solar-system.component';

export const routes: Routes = [
  { path: '', component: SolarSystemComponent }
];
